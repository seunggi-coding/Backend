<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>도서관 - 도서 목록</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <style>
            h2 {
                font-size: 2.5rem;
                font-weight: bold;
                text-align: center;
                margin: 40px 0;
            }
            .book-grid {
                display: grid;
                grid-template-columns: repeat(3, 1fr);
                grid-gap: 40px;
                justify-content: center;
                align-items: center;
            }

            .book-card {
                background-color: #fff;
                border-radius: 8px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                overflow: hidden;
                text-align: center;
                width: 80%;
                margin-left: 30px;
            }

            .book-card img {
                width: 100%;
                height: 300px;
                object-fit: cover;
            }

            .book-card h3 {
                font-size: 1.5rem;
                font-weight: bold;
                margin-top: 20px;
            }

            .book-card p {
                margin-top: 10px;
            }

            .cta {
                display: inline-block;
                background-color: #007bff;
                color: #fff;
                text-decoration: none;
                padding: 12px 24px;
                border-radius: 4px;
                margin: 20px 0;
            }

            .cta:hover {
                background-color: #0056b3;
                color: white;
            }
        </style>
    </head>
    <body>
        <%- include('menu.html') %>

        <main>
            <section class="featured-books">
                <h2>도서 목록</h2>
                <div class="book-grid">
                    <% allBooks.forEach(book => { %>
                        <div class="book-card">
                            <img src="<%= book.img %>" alt="<%= book.title %>">
                            <h3><%= book.title %></h3>
                            <p><%= book.author %></p>
                            <p><%= book.category %></p>
                            <a href="/library/book/<%= book._id %>" class="cta">자세히 보기</a>
                        </div>
                    <% }); %>
                </div>
            </section>
        </main>

        <%- include('footer.html') %>
        
    </body>
</html>
