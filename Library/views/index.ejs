<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>도서관 - 메인 페이지</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <style>
            .hero {
                background-color: #f8f9fa;
                padding: 80px 0;
                text-align: center;
            }
    
            .hero h2 {
                font-size: 3rem;
                font-weight: bold;
                margin-bottom: 20px;
            }
    
            .hero p {
                font-size: 1.5rem;
                margin-bottom: 40px;
            }
    
            .featured-books {
                padding: 80px 50px; /* 패딩 추가 */
            }
    
            .featured-books h2 {
                font-size: 2.5rem;
                font-weight: bold;
                margin-bottom: 40px;
            }
    
            .book-grid {
                display: grid;
                grid-template-columns: repeat(3, 1fr); /* 3개의 열로 구성 */
                grid-gap: 40px;
                justify-content: center; /* 가로 중앙 정렬 */
                align-items: center; /* 세로 중앙 정렬 */
            }

            .book-card {
                background-color: #fff;
                border-radius: 8px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                overflow: hidden;
                text-align: center;
                width: 80%; /* 카드 요소의 width 사이즈 줄임 */
                margin-left: 30px;
            }
    
            .book-card img {
                width: 100%;
                height: 300px;
                object-fit: cover;
            }
    
            .book-card h3 {
                font-size: 1.5rem;
                font-weight: bold;
                margin-top: 20px;
            }
    
            .book-card p {
                margin-top: 10px;
            }
    
            .cta {
                display: inline-block;
                background-color: #007bff;
                color: #fff;
                text-decoration: none;
                padding: 12px 24px;
                border-radius: 4px;
                margin: 20px 0;
            }
    
            .cta:hover {
                background-color: #0056b3;
                color: white;
            }
    
        </style>
    </head>
    <body>
        <% if (user) { %>
            <%- include('menu2.html') %>
        <% } else { %>
            <%- include('menu.html') %>
        <% } %>
        <main>    
            <section class="hero">
                <h2>환영합니다!</h2>
                <p>이 도서관에서 다양한 도서를 만나보세요.</p>
                <a href="/list" class="cta">도서 목록 보기</a>
            </section>
    
            <section class="featured-books">
                <h2>추천 도서</h2>
                <div class="book-grid">
                    <% featuredBooks.forEach(book => { %>
                        <div class="book-card">
                            <img src="<%= book.img %>" alt="<%= book.title %>">
                            <h3><%= book.title %></h3>
                            <p><%= book.author %></p>
                            <p><%= book.category %></p>
                            <% if (user) { %>
                                <a href="/library/book/<%= book._id %>" class="cta">자세히 보기</a>
                            <% } else { %>
                                <a href="/login" class="cta">자세히 보기</a>
                            <% } %>
                        </div>
                    <% }); %>
                </div>
            </section>
        </main>
    
        <%- include('footer.html') %>
    </body>
    </html>
